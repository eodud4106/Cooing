<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cooing.www.joon.dao.AlbumReplyMapper">
<!-- 댓글 작성 -->
<insert id="replyWrite" parameterType="albumreply">
	insert into reply (
		reply_num
		, reply_albumnum
		, reply_memberid
		, reply_contents
	) 
	values (
		reply_seq.nextval
		, #{reply_albumnum}
		, #{reply_memberid}
		, #{reply_contents}
	)
</insert>

<!-- 댓글 삭제 -->
<delete id="replyDelete" parameterType="albumreply">
delete from reply where reply_memberid = #{reply_memberid} and reply_albumnum = #{reply_albumnum}
</delete>
</mapper>
